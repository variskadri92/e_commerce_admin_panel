import 'package:flutter/material.dart';

class MediaMobile extends StatelessWidget {
  const MediaMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
