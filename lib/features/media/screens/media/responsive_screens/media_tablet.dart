import 'package:flutter/material.dart';

class MediaTablet extends StatelessWidget {
  const MediaTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
